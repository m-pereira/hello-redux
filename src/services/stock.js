// import api from './api';

// const stock = async ({ id, onSuccess, onError }) => {
//   try {
//     const response = await api.get(`/stock/${id}`);

//     onSuccess(response.data);
//   } catch (e) {
//     onError(e);
//   }
// };

// export default stock;
